cmake_minimum_required(VERSION 3.0)
project(socket_thread)
set(S_BIN server_thread)
set(C_BIN client_thread)
add_executable(${S_BIN} src/server_thread.c )
add_executable(${C_BIN} src/client_thread.c )
target_link_libraries(${S_BIN} pthread)
target_link_libraries(${C_BIN} pthread)
